server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres?currentSchema=spring-final
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true